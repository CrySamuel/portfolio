node_modules
.next
out
dist
build
coverage
target
.turbo

pnpm-lock.yaml

# Codigo gerado do OpenAPI - reescrito a cada build.
packages/api-client/src/generated

# O plano e documento de referencia, nao codigo-fonte. Reformata-lo produz
# 1.600 linhas de ruido de alinhamento de tabela e polui o diff de qualquer
# revisao futura do conteudo.
docs/PLANO-COMPLETO.md

# Wrapper do Maven: arquivos de terceiros, mantidos como distribuidos.
apps/api/mvnw
apps/api/mvnw.cmd
apps/api/.mvn

# Artefatos do GitHub Copilot App Modernization. Ja sao ignorados pelo git
# (.github/modernize/java-upgrade/.gitignore), mas o Prettier nao le .gitignore -
# so .prettierignore. Sem esta entrada o format:check quebra num arquivo .json
# que na verdade contem JSONL, e a falha nao e nossa nem tem conserto do nosso
# lado: o arquivo e gerado por outra ferramenta.
.github/modernize
